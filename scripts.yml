scripts:
  t:
    desc: Run tests
    cmd: deno test --allow-read --allow-env
    env:
      DENO_ENV: test
  l:
    desc: Run cli
    cmd: deno run --allow-read --allow-env --allow-net src/lang.ts
    env:
      DENO_ENV: dev
